# MongoDB Connection String
# ================================================
# OPTION 1: MongoDB Atlas (RECOMMENDED - Free tier, no setup!)
# 1. Go to https://cloud.mongodb.com
# 2. Create free cluster (M0 Sandbox)
# 3. Create database user
# 4. Get connection string and replace below:
MONGODB_URI="mongodb+srv://USERNAME:PASSWORD@cluster0.xxxxx.mongodb.net/runner_quiz?retryWrites=true&w=majority"

# OPTION 2: Local MongoDB (if you have it installed)
# MONGODB_URI="mongodb://localhost:27017/runner_quiz"

# Server Configuration
PORT=4000

# CORS - Allowed origins (comma-separated for multiple)
# Development:
CORS_ORIGIN="http://localhost:5173,http://localhost:3000"

# Production example:
# CORS_ORIGIN="https://your-frontend-domain.vercel.app"

# Environment
NODE_ENV="development"
